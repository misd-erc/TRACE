@model IEnumerable<TRACE.Models.CaseNature>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nature)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Active)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaseCategory)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nature)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CaseCategory.CaseCategoryId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CaseNatureId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CaseNatureId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CaseNatureId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

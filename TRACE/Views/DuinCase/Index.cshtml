@model IEnumerable<TRACE.Models.DuinCase>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DuId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaseId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DuId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CaseId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DuinCasesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DuinCasesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DuinCasesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
